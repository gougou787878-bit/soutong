<?php include __DIR__ . "/../layout/header.phtml";?>
        <div class="row">
            <div class="col-sm-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>落地页推广统计</h5>
                        <div class="ibox-tools">
                        </div>
                    </div>
                    
                    <div class="ibox-content">
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>日期</th>
                                        <th>官网访问（去重）</th>
                                        <th>官网访问（0）</th>
                                        <th>官网访问（推广）</th>
                                        <th>官网访问（无）</th>
                                        <th>分享访问（去重）</th>
                                        <th>分享人数</th>
                                        <th>新增用户（去重）</th>
                                        <th>新增用户</th>
                                        <th>新增用户（0）</th>
                                        <th>新增用户（分享）</th>
                                        <th>新增用户（推广）</th>
                                        <th>新增用户（无）</th>
                                        <th>活跃用户（去重）</th>
                                        <th>活跃用户</th>
                                        <th>绑定手机</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <?php if($items):?>
                                    <?php foreach ($items as $k=>$v) :?>
                                    <tr>
                                        <td><?=$v['id'];?></td>
                                        <td><?=$v['date'];?></td>
                                        <td><?=$v['index_total'];?></td>
                                        <td><?=$v['index_aff_zero'];?></td>
                                        <td><?=$v['index_aff_advert'];?></td>
                                        <td><?=$v['index_aff_our'];?></td>
                                        <td><?=$v['share_total'];?></td>
                                        <td><?=$v['share_user_count'];?></td>
                                        <td><?=$v['user_add_total'];?></td>
                                        <td><?=$v['user_add_count'];?></td>
                                        <td><?=$v['user_add_aff_zero'];?></td>
                                        <td><?=$v['user_add_aff_share'];?></td>
                                        <td><?=$v['user_add_aff_advert'];?></td>
                                        <td><?=$v['user_add_aff_our'];?></td>
                                        <td><?=$v['active_user_total'];?></td>
                                        <td><?=$v['active_user_count'];?></td>
                                        <td><?=$v['user_bind_phone_count'];?></td>
                                    </tr>
                                    <?php endforeach; ?>
                                    <?php else:?>
                                    <tr>
                                        <td colspan="16">暂无数据</td>
                                    </tr>
                                    <?php endif;?>
                                </tbody>
                            </table>
                        </div>
                        <?=$page_arr['html'];?>
                    </div>
                </div>
            </div>

        </div>
        <script>
            layui.laydate.render({
                elem: '#start'
            })
            layui.laydate.render({
                elem: '#end'
            })
            function showUser(aff) {
                var url = "d.php?mod=user&code=index&aff=" + aff
                layerOpen(url, '用户详情')
            }

            function layerOpen(url, title) {
                parent.layer.open({
                    type: 2,
                    title: title,
                    closeBtn: 1,
                    shadeClose: true,
                    shade: [0.5],
                    area: ['1200px', '500px'],
                    content: url
                })
            }

            function getAjax(url) {
                $.ajax({
                    url: url,
                    type: 'GET',
                    success: function (res) {
                        if (res.status == 1) {
                            layer.msg(res.data, {icon: 1})
                        } else {
                            layer.msg(res.data, {icon: 5})
                        }
                    }
                })
            }

            function reload(msg) {
                layer.msg(msg, {icon: 1})
                setTimeout(function () {
                    window.location.reload()
                }, 3000)
            }
        </script>
        <?php include __DIR__ . "/../layout/footer.phtml";?>
